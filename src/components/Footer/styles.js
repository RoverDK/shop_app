import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
  footer: {
    backgroundColor: "#CCC",
    padding: "10px",
    position: "absolute",
    textAlign: "center",
    width: "100%",
  },
});
