import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
  header: {
    display: "flex",
    justifyContent: "space-around",
    backgroundColor: "#CCC",
    alignItems: "center",
    padding: "5px",
    width: "100%",
  },
});
